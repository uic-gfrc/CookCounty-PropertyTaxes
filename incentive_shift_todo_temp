2. Figure out burden shift just to class 2 OR just properties that claim the GHE!!!!!

3. Discuss other incentive types.

4. Look at the following variables:
    A. current_rate vs. current_rate_mean. Why median? I think we actually COULD use the mode. -> ptaxsim_muni_level_2006-2021.csv

5. Identify useful example municipalities Some visualizations or something.


clean_name, current rate, new rate, left_join(majorclass_sums.csv)

n. compare to excel file
